{
	email {$ACME_EMAIL}
}

{$LIAISON_DOMAIN} {
	encode zstd gzip
	reverse_proxy liaison:3000

	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		X-Content-Type-Options "nosniff"
		Referrer-Policy "same-origin"
	}
}
